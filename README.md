# Medical Imaging and Detection of Renal Tumors Using Deep Learning Methods


### Workflows (which files to update first)
Update config.yaml
Update secrets.yaml [Optional]
Update params.yaml
Update the entity
Update the configuration manager in src config
Update the components
Update the pipeline
Update the main.py (endpoint)
Update the dvc.yaml
app.py (update last)
 